# Usage : $ ruby split_inline_id.rb ../box2d_dll/box2d/include/box2d/id.h
require 'fileutils'

original_lines = nil
inline_functions = {}
current_key = nil
current_body = nil

filename = '../box2d_dll/box2d/include/box2d/id.h'
if ARGV[0] != nil
  filename = ARGV[0]
end

# Copy original id.h
if File.exist?("#{filename}.default")
  FileUtils.copy("#{filename}.default", filename)
end

# Extract inline functions
File.open(filename, 'r') do |id_h|
  original_lines = id_h.readlines
  parsing = false
  original_lines.each do |original_line|
    if original_line.start_with? 'B2_INLINE'
      current_key = original_line.split(/\W()/).reject(&:empty?)[2] # key : "B2_INLINE float b2LengthSquared( b2Vec2 v )" -> "b2LengthSquared"
      first_line = original_line.gsub('B2_INLINE ', '') # first_line : "float b2LengthSquared( b2Vec2 v )"
      current_body = [first_line]
      parsing = true
      next
    end
    if parsing
      current_body << original_line
      if original_line.start_with? '}'
        inline_functions[current_key] = current_body
        parsing = false
        next
      end
    end
  end
end

# Comment out inline functions in original content
modified_lines = []
parsing = false
original_lines.each do |original_line|
  modified_line = nil
  if original_line.start_with? 'B2_INLINE'
    parsing = true
  end
  if parsing
    modified_line = "// #{original_line}"
    if original_line.start_with? '}'
      parsing = false
    end
  end
  modified_lines << (modified_line.nil? ? original_line : modified_line)
end

# Copy original id.h
FileUtils.copy(filename, "#{filename}.default")
FileUtils.copy("#{File.dirname(filename)}/../../src/id_pool.c", "#{File.dirname(filename)}/../../src/id_pool.c.default")

# Write modified version of id.h
File.open(filename, 'w') do |id_h|
  modified_lines.each do |modified_line|
    id_h.puts modified_line
  end
  id_h.puts '#include "id_inline.h"'
end

# Modify id_pool.c to include split code
File.open("#{File.dirname(filename)}/../../src/id_pool.c", 'a') do |id_pool_c|
  id_pool_c.puts '#include "id_inline.c"'
end

# Write id_inline.h and id_inline.c

File.open("#{File.dirname(filename)}/id_inline.h", 'w') do |id_inline_h|
  header = <<-HEADER
// [NOTE] Autogenerated. Do NOT edit.
#pragma once

#include "id.h"

HEADER
  id_inline_h << header
  inline_functions.each do |key, body|
    id_inline_h.puts "#{body[0].chop};"
  end
end

File.open("#{File.dirname(filename)}/../../src/id_inline.c", 'w') do |id_inline_c|
  header = <<-HEADER
// [NOTE] Autogenerated. Do NOT edit.
#include "box2d/id_inline.h"

HEADER
  id_inline_c << header
  inline_functions.each do |key, body|
    id_inline_c.puts body
    id_inline_c.puts
  end
end
